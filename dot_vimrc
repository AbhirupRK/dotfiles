" General config for both VIM and NeoVIM

" Set number
set nowrap
"set mouse=

" Keybindings
nnoremap q :q<CR>
nnoremap Q :qa<CR>
nnoremap L :set number!<CR>
nnoremap W :set wrap!<CR>:echo "Text wraping is now " . (&wrap ? "ON" : "OFF")<CR>
nnoremap <Space> <C-w>w

